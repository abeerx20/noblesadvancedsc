rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // الرفع والتنزيل يتمان من خلال API مع فحص النوع والحجم والصلاحية.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}

